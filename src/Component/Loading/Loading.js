import React from "react";
import { PointSpreadLoading } from "react-loadingg";

const Loading = () => {
  return (
    <div>
      <PointSpreadLoading size="large" color="#ffffff" />
    </div>
  );
};

export default Loading;
